const accountId = 12345
let emailId = "st@gmail.com"
var numberId = "4353436"
let accountstate
// const keyword never changes where as let and var can be changed
//var is not used now a days due to issue in block scope and fucntional scope
// when nothing assigned then undefined is the output 
console.table([accountId,emailId,numberId,accountstate])